info:
  name: Broker data
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/broker/:name
  params:
    - name: name
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
